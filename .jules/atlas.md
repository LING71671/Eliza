## 2025-06-01 - Test Suite Introduction
**Learning:** The project is built with React, Vite, and Typescript, but completely lacked tests for its complex bot logic and UI components. JSDOM requires mocking `HTMLElement.prototype.scrollIntoView`.
**Action:** Introduced Vitest and React Testing Library. Added basic test suite for `ElizaBot.ts` and `App.tsx`. Future changes should maintain or extend this test coverage.
\n## 2026-06-13 - Fix Fallback Test & ID Generation\n**Learning:** The previous fallback test failed because catch-all rules in the bot's script often intercept inputs and substitute placeholders. `Date.now()` is unreliable for unique keys if generated concurrently. Also, `generateId` helper functions should be defined outside component boundaries to prevent recreation on re-render.\n**Action:** Use `crypto.randomUUID()` with a fallback for message IDs. Keep `generateId` outside component logic. Ensure tests that test generic bot inputs check both regular fallbacks and formatted catch-all rules rather than resorting to weak `toBeTypeOf` checks.
